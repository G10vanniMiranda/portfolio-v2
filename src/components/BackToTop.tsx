"use client";
// Componente removido: BackToTop
// Este arquivo foi mantido apenas como marcador para indicar que o botão de
// "Voltar ao topo" foi removido do projeto por solicitação. Não é usado em
// nenhum lugar e pode ser apagado com segurança.

export default function BackToTop() {
  // Retorna null para não renderizar nada caso alguém importe por engano.
  return null;
}
